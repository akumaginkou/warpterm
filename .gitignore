/target
**/target
Cargo.lock

# frontend
frontend/node_modules
frontend/dist
frontend/.vite

# e2e (WebDriver) deps
e2e/node_modules

# tauri generated
src-tauri/gen
src-tauri/target

# WARP account state / secrets — never commit
warp-state/
*.warp-config.json
account-*.json

# app icons are committed so the app builds out of the box
# (regenerate with `cargo tauri icon path/to/icon.png`)

# tsc type-check only (Vite bundles); ignore any emitted JS
frontend/src/**/*.js
